该模块是 .trae/ 下的 IDE 级配置目录，采用扁平结构：.ignore 文件定义被 Trae 忽略的文件模式；documents 和 rules 子目录用于存放项目文档与规则（当前为空）；efcore magrations.sh 是一个独立的 shell 脚本，封装了 EF Core 的 migrations add 与 database update 命令，通过 --context AuditDbContext 指定上下文并输出到 Migrations 目录。模块不依赖其他代码，仅作为开发环境辅助配置存在。