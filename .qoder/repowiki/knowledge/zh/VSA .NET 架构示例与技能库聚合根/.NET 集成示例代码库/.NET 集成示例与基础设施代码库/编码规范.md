- Options 类采用 POCO 属性 + 默认值初始化（如 Enabled=true、MaxRetryCount=3），并通过构造函数或 DI 注入到服务中。
- 异步服务类普遍实现 IAsyncDisposable，在 DisposeAsync 中释放外部资源（如 Kafka Producer.Flush/Dispose）。
- SourceGenerator 示例统一实现 ISourceGenerator 接口，配合 ISyntaxContextReceiver 收集语法节点并通过 context.AddSource 输出 g.cs 文件。
- Blazor 组件使用 #:sdk Microsoft.NET.Sdk.Razor 指令头，并通过 @page 路由参数与 @inject 注入服务。
- 所有示例文件顶部以 #:sdk/#:package/#:property 元数据声明目标框架、语言版本和依赖包，便于自动化构建。