示例通过文件头部的 `#:sdk`、`#:package`、`#:property` 元数据声明依赖（如 `#:sdk Microsoft.NET.Sdk.Razor`、`#:package Microsoft.AspNetCore.Components.Web@8.0.0`），由构建系统自动解析并恢复包，无需手动编辑 csproj。部分示例需要外部服务（如 Keycloak、Redis、SQL Server LocalDB），需先启动对应基础设施并按 appsettings.json 中的连接字符串配置。