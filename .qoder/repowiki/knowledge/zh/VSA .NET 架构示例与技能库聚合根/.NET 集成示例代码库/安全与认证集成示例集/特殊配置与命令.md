每个示例文件通过文件头部的 `#:package` 指令声明 NuGet 包依赖，`#:property TargetFramework net10.0` 指定目标框架，`#:property PublishAot true` 可选启用 AOT 编译。运行方式通常为 `dotnet run --project <file>.cs`，但需要确保已安装 .NET 10 SDK。appsettings.json 包含完整的配置模板，包括数据库连接字符串、Redis 配置、Kestrel 端点、JWT 密钥等。