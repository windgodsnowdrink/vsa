使用 `dotnet run --project Feature.csproj` 启动，默认监听 `http://plc.dev.localhost:5202` 与 `https://plc.dev.localhost:7276`；需确保 Aspire Dashboard 在 `localhost:21456`/`22668` 运行。原生库通过 `cmake -S plc/nativelib -B plc/nativelib/out` 生成 `greetings` 动态库后由 C# P/Invoke 加载。